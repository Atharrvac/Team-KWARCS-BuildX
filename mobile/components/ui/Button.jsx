/**\n * 🎨 RECIFY BUTTON COMPONENT\n * Beautiful, accessible, production-ready button\n */\n\nimport React from 'react';\nimport { TouchableOpacity, Text, StyleSheet, ActivityIndicator, View } from 'react-native';\nimport { DESIGN_SYSTEM } from '../../constants/designSystem';\n\nconst { colors, typography, spacing, shadows, componentSizes, borderRadius } = DESIGN_SYSTEM;\n\nexport const Button = React.forwardRef(\n  (\n    {\n      label,\n      onPress,\n      variant = 'primary',\n      size = 'md',\n      disabled = false,\n      loading = false,\n      icon = null,\n      fullWidth = false,\n      style = {},\n      textStyle = {},\n      ...props\n    },\n    ref\n  ) => {\n    const buttonSize = componentSizes.button[size];\n    const isDisabled = disabled || loading;\n\n    const variantStyles = {\n      primary: {\n        backgroundColor: colors.primary[500],\n        borderColor: colors.primary[500],\n      },\n      secondary: {\n        backgroundColor: colors.secondary[500],\n        borderColor: colors.secondary[500],\n      },\n      outline: {\n        backgroundColor: 'transparent',\n        borderColor: colors.primary[500],\n        borderWidth: 2,\n      },\n      ghost: {\n        backgroundColor: 'transparent',\n        borderColor: 'transparent',\n      },\n      danger: {\n        backgroundColor: colors.error,\n        borderColor: colors.error,\n      },\n    };\n\n    const buttonStyle = [\n      styles.button,\n      buttonSize,\n      variantStyles[variant],\n      isDisabled && styles.disabled,\n      fullWidth && styles.fullWidth,\n      style,\n    ];\n\n    const textColor = variant === 'outline' || variant === 'ghost' ? colors.primary[500] : colors.neutral[0];\n\n    return (\n      <TouchableOpacity\n        ref={ref}\n        style={buttonStyle}\n        onPress={onPress}\n        disabled={isDisabled}\n        activeOpacity={0.7}\n        {...props}\n      >\n        <View style={styles.content}>\n          {loading ? (\n            <ActivityIndicator color={textColor} size=\"small\" />\n          ) : (\n            <>\n              {icon && <View style={styles.icon}>{icon}</View>}\n              <Text\n                style={[\n                  styles.text,\n                  { color: textColor },\n                  typography.presets.button,\n                  textStyle,\n                ]}\n              >\n                {label}\n              </Text>\n            </>\n          )}\n        </View>\n      </TouchableOpacity>\n    );\n  }\n);\n\nButton.displayName = 'Button';\n\nconst styles = StyleSheet.create({\n  button: {\n    borderRadius: borderRadius.md,\n    justifyContent: 'center',\n    alignItems: 'center',\n    ...shadows.md,\n  },\n  content: {\n    flexDirection: 'row',\n    alignItems: 'center',\n    justifyContent: 'center',\n  },\n  text: {\n    fontWeight: typography.weights.semibold,\n  },\n  icon: {\n    marginRight: spacing[2],\n  },\n  disabled: {\n    opacity: 0.5,\n  },\n  fullWidth: {\n    width: '100%',\n  },\n});\n\nexport default Button;\n