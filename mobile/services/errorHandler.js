/**\n * 🛡️ RECIFY ERROR HANDLING & LOGGING\n * Production-grade error management\n */\n\nimport * as Sentry from 'sentry-expo';\n\n// ============================================================================\n// ERROR TYPES & CLASSIFICATION\n// ============================================================================\n\nexport const ERROR_TYPES = {\n  // Network Errors\n  NETWORK_ERROR: 'NETWORK_ERROR',\n  TIMEOUT_ERROR: 'TIMEOUT_ERROR',\n  CONNECTION_ERROR: 'CONNECTION_ERROR',\n  OFFLINE_ERROR: 'OFFLINE_ERROR',\n\n  // Authentication Errors\n  AUTH_ERROR: 'AUTH_ERROR',\n  UNAUTHORIZED: 'UNAUTHORIZED',\n  FORBIDDEN: 'FORBIDDEN',\n  SESSION_EXPIRED: 'SESSION_EXPIRED',\n\n  // Validation Errors\n  VALIDATION_ERROR: 'VALIDATION_ERROR',\n  INVALID_INPUT: 'INVALID_INPUT',\n  MISSING_FIELD: 'MISSING_FIELD',\n\n  // Server Errors\n  SERVER_ERROR: 'SERVER_ERROR',\n  NOT_FOUND: 'NOT_FOUND',\n  CONFLICT: 'CONFLICT',\n  RATE_LIMITED: 'RATE_LIMITED',\n\n  // Client Errors\n  CLIENT_ERROR: 'CLIENT_ERROR',\n  PARSE_ERROR: 'PARSE_ERROR',\n  STORAGE_ERROR: 'STORAGE_ERROR',\n  PERMISSION_ERROR: 'PERMISSION_ERROR',\n\n  // Unknown Errors\n  UNKNOWN_ERROR: 'UNKNOWN_ERROR',\n};\n\n// ============================================================================\n// ERROR SEVERITY LEVELS\n// ============================================================================\n\nexport const ERROR_SEVERITY = {\n  CRITICAL: 'critical', // App-breaking errors\n  ERROR: 'error', // Significant errors\n  WARNING: 'warning', // Non-critical issues\n  INFO: 'info', // Informational messages\n  DEBUG: 'debug', // Debug information\n};\n\n// ============================================================================\n// CUSTOM ERROR CLASS\n// ============================================================================\n\nexport class RecifyError extends Error {\n  constructor({\n    type = ERROR_TYPES.UNKNOWN_ERROR,\n    message = 'An unknown error occurred',\n    severity = ERROR_SEVERITY.ERROR,\n    statusCode = 500,\n    originalError = null,\n    context = {},\n    userMessage = null,\n    recoveryAction = null,\n  } = {}) {\n    super(message);\n    this.name = 'RecifyError';\n    this.type = type;\n    this.severity = severity;\n    this.statusCode = statusCode;\n    this.originalError = originalError;\n    this.context = context;\n    this.userMessage = userMessage || this.getDefaultUserMessage();\n    this.recoveryAction = recoveryAction;\n    this.timestamp = new Date().toISOString();\n    this.stack = Error.captureStackTrace(this, this.constructor);\n  }\n\n  getDefaultUserMessage() {\n    const messages = {\n      [ERROR_TYPES.NETWORK_ERROR]: 'Network connection failed. Please check your internet.',\n      [ERROR_TYPES.TIMEOUT_ERROR]: 'Request timed out. Please try again.',\n      [ERROR_TYPES.OFFLINE_ERROR]: 'You are offline. Some features may be limited.',\n      [ERROR_TYPES.AUTH_ERROR]: 'Authentication failed. Please log in again.',\n      [ERROR_TYPES.UNAUTHORIZED]: 'You are not authorized to perform this action.',\n      [ERROR_TYPES.SESSION_EXPIRED]: 'Your session has expired. Please log in again.',\n      [ERROR_TYPES.VALIDATION_ERROR]: 'Please check your input and try again.',\n      [ERROR_TYPES.SERVER_ERROR]: 'Server error. Please try again later.',\n      [ERROR_TYPES.NOT_FOUND]: 'The requested item was not found.',\n      [ERROR_TYPES.RATE_LIMITED]: 'Too many requests. Please wait a moment.',\n    };\n    return messages[this.type] || 'Something went wrong. Please try again.';\n  }\n\n  toJSON() {\n    return {\n      type: this.type,\n      message: this.message,\n      severity: this.severity,\n      statusCode: this.statusCode,\n      userMessage: this.userMessage,\n      recoveryAction: this.recoveryAction,\n      timestamp: this.timestamp,\n      context: this.context,\n    };\n  }\n}\n\n// ============================================================================\n// ERROR HANDLER SERVICE\n// ============================================================================\n\nexport class ErrorHandler {\n  static initialize() {\n    // Initialize Sentry for error tracking\n    if (process.env.NODE_ENV === 'production') {\n      Sentry.init({\n        dsn: process.env.SENTRY_DSN,\n        environment: process.env.NODE_ENV,\n        tracesSampleRate: 0.1,\n        attachStacktrace: true,\n        maxBreadcrumbs: 100,\n        beforeSend: this.beforeSend,\n      });\n    }\n  }\n\n  static beforeSend(event, hint) {\n    // Filter sensitive information\n    if (event.request) {\n      delete event.request.cookies;\n      delete event.request.headers['Authorization'];\n    }\n\n    // Don't send certain errors\n    if (hint.originalException?.message?.includes('Network request failed')) {\n      return null;\n    }\n\n    return event;\n  }\n\n  static handle(error, context = {}) {\n    const recifyError = this.normalize(error);\n\n    // Log the error\n    this.log(recifyError, context);\n\n    // Report to Sentry in production\n    if (process.env.NODE_ENV === 'production') {\n      Sentry.captureException(recifyError, {\n        contexts: { app: context },\n        level: recifyError.severity,\n      });\n    }\n\n    // Return normalized error\n    return recifyError;\n  }\n\n  static normalize(error) {\n    // Already a RecifyError\n    if (error instanceof RecifyError) {\n      return error;\n    }\n\n    // Network errors\n    if (error?.message?.includes('Network') || error?.message?.includes('fetch')) {\n      return new RecifyError({\n        type: ERROR_TYPES.NETWORK_ERROR,\n        message: error.message,\n        severity: ERROR_SEVERITY.ERROR,\n        originalError: error,\n      });\n    }\n\n    // Timeout errors\n    if (error?.message?.includes('timeout') || error?.code === 'ETIMEDOUT') {\n      return new RecifyError({\n        type: ERROR_TYPES.TIMEOUT_ERROR,\n        message: 'Request timed out',\n        severity: ERROR_SEVERITY.WARNING,\n        originalError: error,\n      });\n    }\n\n    // HTTP errors\n    if (error?.status) {\n      return this.handleHttpError(error);\n    }\n\n    // Parse errors\n    if (error instanceof SyntaxError) {\n      return new RecifyError({\n        type: ERROR_TYPES.PARSE_ERROR,\n        message: 'Failed to parse response',\n        severity: ERROR_SEVERITY.ERROR,\n        originalError: error,\n      });\n    }\n\n    // Default\n    return new RecifyError({\n      type: ERROR_TYPES.UNKNOWN_ERROR,\n      message: error?.message || 'An unknown error occurred',\n      severity: ERROR_SEVERITY.ERROR,\n      originalError: error,\n    });\n  }\n\n  static handleHttpError(error) {\n    const { status, statusText, data } = error;\n\n    const errorMap = {\n      400: { type: ERROR_TYPES.VALIDATION_ERROR, severity: ERROR_SEVERITY.WARNING },\n      401: { type: ERROR_TYPES.UNAUTHORIZED, severity: ERROR_SEVERITY.ERROR },\n      403: { type: ERROR_TYPES.FORBIDDEN, severity: ERROR_SEVERITY.ERROR },\n      404: { type: ERROR_TYPES.NOT_FOUND, severity: ERROR_SEVERITY.WARNING },\n      409: { type: ERROR_TYPES.CONFLICT, severity: ERROR_SEVERITY.WARNING },\n      429: { type: ERROR_TYPES.RATE_LIMITED, severity: ERROR_SEVERITY.WARNING },\n      500: { type: ERROR_TYPES.SERVER_ERROR, severity: ERROR_SEVERITY.ERROR },\n      503: { type: ERROR_TYPES.SERVER_ERROR, severity: ERROR_SEVERITY.ERROR },\n    };\n\n    const errorInfo = errorMap[status] || {\n      type: ERROR_TYPES.SERVER_ERROR,\n      severity: ERROR_SEVERITY.ERROR,\n    };\n\n    return new RecifyError({\n      ...errorInfo,\n      message: data?.message || statusText || 'HTTP Error',\n      statusCode: status,\n      originalError: error,\n      context: { httpStatus: status, responseData: data },\n    });\n  }\n\n  static log(error, context = {}) {\n    const logEntry = {\n      timestamp: new Date().toISOString(),\n      type: error.type,\n      severity: error.severity,\n      message: error.message,\n      context: { ...error.context, ...context },\n      stack: error.stack,\n    };\n\n    // Log to console in development\n    if (process.env.NODE_ENV === 'development') {\n      console.error('[ERROR]', logEntry);\n    }\n\n    // Store in local storage for debugging\n    this.storeErrorLog(logEntry);\n  }\n\n  static storeErrorLog(logEntry) {\n    try {\n      const logs = JSON.parse(localStorage.getItem('error_logs') || '[]');\n      logs.push(logEntry);\n      // Keep only last 100 errors\n      if (logs.length > 100) {\n        logs.shift();\n      }\n      localStorage.setItem('error_logs', JSON.stringify(logs));\n    } catch (e) {\n      console.error('Failed to store error log', e);\n    }\n  }\n\n  static getErrorLogs() {\n    try {\n      return JSON.parse(localStorage.getItem('error_logs') || '[]');\n    } catch (e) {\n      return [];\n    }\n  }\n\n  static clearErrorLogs() {\n    try {\n      localStorage.removeItem('error_logs');\n    } catch (e) {\n      console.error('Failed to clear error logs', e);\n    }\n  }\n}\n\n// ============================================================================\n// GLOBAL ERROR HANDLERS\n// ============================================================================\n\nexport function setupGlobalErrorHandlers() {\n  // Unhandled promise rejections\n  if (typeof window !== 'undefined') {\n    window.addEventListener('unhandledrejection', (event) => {\n      ErrorHandler.handle(event.reason, { source: 'unhandledRejection' });\n    });\n\n    // Global error handler\n    window.addEventListener('error', (event) => {\n      ErrorHandler.handle(event.error, { source: 'globalError' });\n    });\n  }\n}\n\n// ============================================================================\n// RETRY LOGIC\n// ============================================================================\n\nexport async function retryWithBackoff(\n  fn,\n  maxRetries = 3,\n  initialDelay = 1000,\n  maxDelay = 10000\n) {\n  let lastError;\n\n  for (let attempt = 0; attempt < maxRetries; attempt++) {\n    try {\n      return await fn();\n    } catch (error) {\n      lastError = error;\n\n      // Don't retry on certain errors\n      if (\n        error instanceof RecifyError &&\n        [ERROR_TYPES.UNAUTHORIZED, ERROR_TYPES.FORBIDDEN, ERROR_TYPES.VALIDATION_ERROR].includes(\n          error.type\n        )\n      ) {\n        throw error;\n      }\n\n      // Calculate delay with exponential backoff\n      if (attempt < maxRetries - 1) {\n        const delay = Math.min(initialDelay * Math.pow(2, attempt), maxDelay);\n        await new Promise((resolve) => setTimeout(resolve, delay));\n      }\n    }\n  }\n\n  throw lastError;\n}\n\n// ============================================================================\n// EXPORT\n// ============================================================================\n\nexport default ErrorHandler;\n